# Dashboard: the whole folder is tracked. The ONLY exclusion is the parcel
# GeoJSON (user data — large, may carry personal fields), incl. backups.
dashboard/*.geojson*

# Python bytecode
__pycache__/
*.pyc
